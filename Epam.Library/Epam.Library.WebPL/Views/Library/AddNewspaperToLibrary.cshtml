@model NewspaperViewModel

@{
    ViewBag.Title = "Add newspaper";
    Layout = "_Layout";
}

@using (Html.BeginForm("AddNewspaperToLibrary", "Library", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, string.Empty, new { @class = "text-danger" })
    @Html.Hidden("returnUrl", (string)ViewBag.ReturnUrl)
    <div class="mb-3">
        @Html.LabelFor(model => model.Name, new {htmlAttributes = new {@class="form-label"}})
        @Html.EditorFor(model => model.Name, new {htmlAttributes = new {@class="form-control"}})
        @Html.ValidationMessageFor(model => model.Name)
    </div>
    <div class="mb-3">
        @Html.LabelFor(model => model.ISSN, new {htmlAttributes = new {@class="form-label"}})
        @Html.EditorFor(model => model.ISSN, new {htmlAttributes = new {@class="form-control"}})
        @Html.ValidationMessageFor(model => model.ISSN)
    </div>
    <button type="submit" class="btn btn-primary">Add</button>
}
